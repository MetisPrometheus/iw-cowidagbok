[project]
name = "arvid-dagbok-dashboard"
version = "0.1.0"
description = "Streamlit-app for å søke i Arvids arbeidsdagbok"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.36",
    "pandas>=2.0",
    "openpyxl>=3.1",
    "rapidfuzz>=3.6",
]

[tool.uv]
package = false
